import { Cinzel } from "next/font/google";

/** Inscriptional serif, cut from Roman stone capitals — headings and fate lines only, never body copy. */
export const serifFont = Cinzel({ subsets: ["latin"], weight: ["600", "700"] });
